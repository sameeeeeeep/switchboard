{
  "name": "@relay/demo-site",
  "version": "0.0.0",
  "private": true,
  "description": "Add BYO-Claude in 5 lines — a static demo page that uses the visitor's own Claude via @relay/sdk.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "serve": "node serve.mjs",
    "dev": "node build.mjs && node serve.mjs"
  },
  "dependencies": {
    "@relay/sdk": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
