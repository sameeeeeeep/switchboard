{
  "name": "@relay/example-apps",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Switchboard App Store — apps that run on the visitor's own Claude + tools.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "serve": "node serve.mjs",
    "dev": "node build.mjs && node serve.mjs"
  },
  "dependencies": {
    "@relay/sdk": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
